import React, { useContext, useEffect, useState } from 'react';

const Home = ({
  setActiveView,
}) => (
        <>
            <section className="sketch-book-home">
                <p>Home Page</p>
            </section>
        </>
);

export default Home;
